# Initialize a list.
l = [0, 1, 4, 9, 16]
print(l)

# Indexing by index (i.e., position in the list).
print(l[0], l[4], l[-1], l[-2])

# Modify an elemeny of the list.
l[0] = -1
print(l)

# Slicing (this is cool, isn't it?).
print(l[:2], l[2:], l[1:3])
