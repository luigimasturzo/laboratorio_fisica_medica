# Initialize an empty list.
l = []
print(l, len(l))

# Append an element at the end of the list.
l.append(1)

# Concatenate two lists.
l += [2.0, 'tre']
print(l, len(l))

# Create a list in place.
l = [1, 2.0, 'tre']
print(l, len(l))
